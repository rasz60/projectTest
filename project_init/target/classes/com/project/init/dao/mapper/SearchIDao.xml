<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
      "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.project.init.dao.SearchIDao">

	<!-- 검색어 필터 /해시태그 -->
   <select id="search" parameterType="com.project.init.dto.SearchDto" resultType="com.project.init.dto.PostDto">
      SELECT * FROM POST WHERE ${keyword} LIKE '%'||#{searchVal}||'%' 
   </select>

</mapper>